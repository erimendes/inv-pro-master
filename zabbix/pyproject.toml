[project]
name = "zabbix"
version = "0.1.0"
description = "API de integracao e exportacao do Zabbix"
authors = [
    {name = "erimendes",email = "eri.mendes32@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests (>=2.34.2,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
